name: SkillSpector Security Scan

on:
  push:
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        run: python -m pip install -q uv

      - name: Install SkillSpector
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          uv tool install 'git+https://github.com/NVIDIA/skillspector.git'

      - name: Run static security scan
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          skillspector scan . --no-llm --format sarif    --output report.sarif || true
          skillspector scan . --no-llm --format markdown --output report.md || true

      - name: Upload reports
        uses: actions/upload-artifact@v4
        with:
          name: skillspector-reports
          path: |
            report.sarif
            report.md
          if-no-files-found: warn
